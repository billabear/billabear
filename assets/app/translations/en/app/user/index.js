import {USER_SETTINGS_TRANSLATIONS} from "./settings";
import {USER_INVITE_TRANSLATIONS} from "./invite";

export const USER_TRANSLATIONS = {
    settings: USER_SETTINGS_TRANSLATIONS,
    invite: USER_INVITE_TRANSLATIONS,
}